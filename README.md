# spot-letter-swaps-indices
This function is meant to identify swapped letters in a string, and return the indices. If more than one swap is detected, or no swap is detected, the result -1 will be returned. 
